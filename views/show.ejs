<%include ./partials/header.ejs%>
<div class="w-full min-h-screen bg-zinc-800 text-white flex items-center justify-center">
    <div class="w-[100vh] min-h-screen  rounded-lg overflow-hidden flex flex-col items-center justify-center">
        <h1 class="text-3xl font-semibold mt-3 items-start justify-start p-3"><%= post.title %></h1>
        <img class="w-full min-h-screen object-cover" src="/images/uploads/<%= post.image %>">
        <h4 class="text-xl font-semibold mt-3"><%= post.description %></h4>
    </div>
</div>
<%include ./partials/footer.ejs%>